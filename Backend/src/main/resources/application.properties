# Server Configuration
server.port=8080

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://username:password@financemanager.xlt33qk.mongodb.net/?retryWrites=true&w=majority&appName=FinanceManager
spring.data.mongodb.database=wealthtracker

# JWT Configuration
jwt.secret=your-secret-key-change-this-in-production-minimum-256-bits
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://localhost:9000

# Logging
logging.level.com.wealthtracker=DEBUG
logging.level.org.springframework.security=DEBUG

# Toggle JWT authentication: true to enable, false to disable
jwt.enabled=true

# Ollama Configuration (local LLM - llama3.2)
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.model=llama3.2
spring.ai.ollama.chat.options.temperature=0.7
spring.ai.ollama.chat.options.num-predict=2000

# Email Configuration (SMTP)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Frontend URL for password reset links
app.frontend-url=${FRONTEND_URL:http://localhost:5173}

