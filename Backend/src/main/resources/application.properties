# Server Configuration
server.port=8080

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://amit243:Dangerous@cluster0.cagt2oj.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.database=wealthtracker

# JWT Configuration
jwt.secret=your-secret-key-change-this-in-production-minimum-256-bits
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://localhost:9000

# Logging
logging.level.com.wealthtracker=DEBUG
logging.level.org.springframework.security=DEBUG

# Toggle JWT authentication: true to enable, false to disable
jwt.enabled=false

